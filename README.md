This system was written in python so python has to be installed (https://www.python.org/downloads/)
Pip should also be installed

Venv 
## For the scraper, following libraries have to be downloaded:
1. BeautifulSoup (pip install beautifulsoup4)
2. psycopg2 (pip install regex)

## For the database,
1. install postgres 

## For the API, following libraries have to be installed:
1. Flask (pip install Flask) 

## For the dashboard, 
1. Streamlit (pip install streamlit)
2. Folium (pip install folium)
3. pandas (pip install pandas)

